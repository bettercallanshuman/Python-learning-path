# Label the program written in problem 4 with comments.  

# Author: Anshuman Das
# Date: 2026-04-26
# PS5: Labeling the directory lister program with comments

import os  # Importing the built-in 'os' module to interact with the operating system


os.getcwd() # The getcwd() function returns the current working directory as a string.
print(os.listdir()) # The listdir() function returns a list of the names of the entries in the directory given by path. If no path is specified, it defaults to the current working directory. Here, it will print the contents of the current directory.  